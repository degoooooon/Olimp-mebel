// ─── Общее состояние приложения ───
// Эти структуры делят между собой каталог и корзина.
import { PRODUCTS } from './data.js';

// Ключ localStorage; версия в имени спасёт от старого формата при изменениях.
const CART_KEY = 'olimp-cart-v1';

const KNOWN_IDS = new Set(PRODUCTS.map((p) => p.id));

// Читает сохранённую корзину. localStorage может быть недоступен (приватный
// режим) или содержать мусор — тогда начинаем с пустой корзины. Записи с
// неизвестным id или кривым количеством отбрасываем.
function loadCart() {
  try {
    const entries = JSON.parse(localStorage.getItem(CART_KEY) || '[]');
    return new Map(entries.filter(([id, qty]) => KNOWN_IDS.has(id) && Number.isInteger(qty) && qty > 0));
  } catch {
    // битые данные или нет доступа к хранилищу
    return new Map();
  }
}

/** @type {Map<number, number>} id товара → количество */
export const cart = loadCart();

/**
 * Сохраняет корзину в localStorage. Зовётся после каждого изменения cart.
 *
 * Молча ничего не делает, если хранилище недоступно: в приватном режиме
 * запись бросает исключение, и без перехвата оно унесло бы с собой всё
 * добавление товара — а корзина в памяти при этом работает.
 *
 * @returns {void}
 */
export function saveCart() {
  try {
    localStorage.setItem(CART_KEY, JSON.stringify([...cart]));
  } catch {
    // приватный режим или переполненное хранилище — просто не сохраняем
  }
}

// ─── Каналы приёма заказов ───
// WhatsApp намеренно не используется: с декабря 2025 Роскомнадзор заблокировал
// его в России, домены исключены из НСДИ. Ссылка вида wa.me у покупателя
// просто не откроется, и заказ до магазина не дойдёт.

// Телефон для звонка. Формат 7XXXXXXXXXX, без «+» и пробелов.
// Тот же номер показан в подвале — при смене правь оба места.
export const ORDER_PHONE = '79620018222';

// Ссылка на профиль магазина в MAX целиком — её даёт сам мессенджер
// в профиле («Поделиться» / «Скопировать ссылку»), вид max.ru/u/<токен>.
// Пусто — кнопка «Написать в MAX» не показывается.
//
// Открыть чат с готовым текстом, как было в wa.me, MAX не умеет: ссылки
// «написать на номер» у него нет вовсе, а профиль открывается без сообщения.
// Поэтому по клику список заказа кладём в буфер обмена — покупателю остаётся
// вставить его в открывшийся чат.
export const MAX_LINK = 'https://max.ru/u/f9LHodD0cOLisUpMJE-ED8v12sofyDFMOM_cwKS210Zd_F0vBkqmk9mA9mU';
